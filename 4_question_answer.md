Railsでは、命名規則に基づいてファイルやクラス間の連携が機能しているため、命名規則を守って開発する必要があります。
名称に「単数」と「複数」が使い分けられているのは、そのファイルやパラメーターに関連する要素が複数存在するかどうかによって決められています。
例えば、modelクラスはテーブルにおける設計書のようなものであるため「単数」で命名されます。
一方、テーブルは複数件の情報を保持するため「複数」で命名されます。
